# Copyright © Advanced Micro Devices, Inc., or its affiliates.
#
# SPDX-License-Identifier: MIT

[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wholebrainseg_large_unest_segmentation-aim"
version = "0.1.0"
requires-python = ">=3.10,<3.13"
description = "AIM serving wrapper for the MONAI wholeBrainSeg_Large_UNEST_segmentation bundle"
dependencies = [
    "bentoml>=1.1.0",
    "pydantic>=2.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = [
    "service",
    "harness",
    "monai_harness",
    "pipeline",
    "config",
    "schemas",
]
